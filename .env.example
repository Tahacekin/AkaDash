# Atlas LMS (Moodle). Kopyalayıp .env yapın; .env git'e eklenmez.
MOODLE_URL=https://mylms.atlas.edu.tr
# Kullanıcı profili > Güvenlik > web hizmeti anahtarları (siteye göre değişir).
# Hizmete eklenecek fonksiyonlar: core_webservice_get_site_info,
# core_enrol_get_users_courses, mod_assign_get_assignments
MOODLE_TOKEN=
